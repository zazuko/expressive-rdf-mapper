language-tags {
	de
	en
	fr
	it
}

vocabulary schema {
	prefix "schema" "https://schema.org/"

	classes
		GovernmentPermit
		GovernmentOrganization

	properties
		name
		alternateName
		description
}

vocabulary ex {
	prefix "ex" "https://schema.example.org/"

	classes

	properties
		agency
		permitLevel
		status
}

